# ~/.tmuxinator/localhost.yml
# you can make as many tabs as you wish...

project_name: localhost
project_root: ~/
pre-window: rvm use default@global
windows:
  - main:
      layout: even-vertical
      panes: 
        - #empty
        - sudo apt-get update && sudo apt-get -y upgrade
  - davos: ssh brandon@davos.blrice.net
